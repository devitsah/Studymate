name: studymate
description: A Flutter project for study planning and tracking
publish_to: 'none'

# ✅ Add this section if it's missing or incorrect
environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  fl_chart: ^0.65.0
  flutter_local_notifications: ^16.3.0
  timezone: ^0.9.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
